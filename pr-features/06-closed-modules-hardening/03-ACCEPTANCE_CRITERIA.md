# Acceptance Criteria

- build Angular de produccion exitoso
- specs agregadas para:
  - `usuarioService`
  - `cajasServices`
  - `VehiculosService`
- los componentes tocados ya no leen el payload legacy crudo para estos flujos
